# alias production="gcloud config configurations activate production && gcloud container clusters get-credentials production-europe-west1-c --zone=europe-west1-c"

# alias production-regional="gcloud config configurations activate production && gcloud container clusters get-credentials production-europe-west1 --zone=europe-west1"

# alias staging="gcloud config configurations activate staging && gcloud container clusters get-credentials staging-europe-west1-c --zone=europe-west1-c"

# alias development="gcloud config configurations activate development && gcloud container clusters get-credentials development-europe-west1-c --zone=europe-west1-c"

# alias bi-test="gcloud config configurations activate bi-test && gcloud container clusters get-credentials travix-bi-test --zone=europe-west1-d"

# alias bi-production="gcloud config configurations activate bi-production"

# alias tooling="gcloud config configurations activate tooling && gcloud container clusters get-credentials tooling --zone=europe-west1-c"

# alias dev-pricing="gcloud config configurations activate dev-pricing && gcloud container clusters get-credentials trvx-dev-pricing --zone=europe-west1-c"

# alias stg-common="gcloud config configurations activate stg-common && gcloud container clusters get-credentials stg-common --zone=europe-west1"

# alias prd-common="gcloud config configurations activate prd-common && gcloud container clusters get-credentials prd-common --zone=europe-west1"

# alias dev-datascience="gcloud config configurations activate dev-datascience && gcloud container clusters get-credentials dev-datascience --zone=europe-west1"

# alias prd-datalake-governance="gcloud config configurations activate prd-datalake-governance"