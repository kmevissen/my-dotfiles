from __future__ import absolute_import

import setuptools

setuptools.setup(
    name='farestore-research',
    version='0.0.1',
    description='Farestore Research Beam Job',
    packages=setuptools.find_packages(),
)
