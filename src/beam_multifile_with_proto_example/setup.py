from __future__ import absolute_import
import setuptools


setuptools.setup(
    name='multiline-example',
    version='0.0.1',
    description='Multiline apache beam python example',
    # install_requires=REQUIRED_PACKAGES,
    packages=setuptools.find_packages(),
)